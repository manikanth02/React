1.We write attribute in camelCase in JSX (like tabIndex = "1");

2.When we write single line JSX then it is not require to wrap with brackets but when we write multiple line JSX then we must wrap with parenthesis 
 WHy? ^^

to make babel understands that it is JSX.

What is role of Babel?
Babel converts ES6 or modern javascripts to older javascripts which is compatible to all browsers

Role of Babel in JSX

In React we write JSX and JSX is js syntax by which we write React.createElements and Babel again converts JSX to React.createElements

```
const element = <h1>Hello, world!</h1>;

```

```
Babel Converts this to
const element = React.createElement('h1', null, 'Hello, world!');

```
17/01/2025


React is all about components -> We make components in React and use it everywhere.

There are two ways to create components
-> 1.Functioanl Componnets -> it used javascript function to create components
-> 2.Class Based Componnets -> it used javascript classes to create components

React Component is a javascript function.

Q.What is difference  b/w normal function and Functional Components?

normal function is normal function but Functional Components returns JSX.



Ways to write functional Components.

const HeadingComponents = () =>(
<div>
<h1>Hello from h1 div</h1>
<h2>Hello from 2nd div</h2>
</div>
);

const HeadingComponents = () =>{
return(
<div>
Hello from 2nd div
</div> 
)}

Q.What is difference between React Elements and React Components ?

root.reder(HeadingComponents) ❌
root.render(<HeadingComponents />)✅


Q,What is component Composition?

Combining two or more than  two  components -> This is called Component Composition


