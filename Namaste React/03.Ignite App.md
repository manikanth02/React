1.We write attribute in camelCase in JSX (like tabIndex = "1");

2.When we write single line JSX then it is not require to wrap with brackets but when we write multiple line JSX then we must wrap with parenthesis 
 WHy? ^^

to make babel understands that it is JSX.

What is role of Babel?
Babel converts ES6 or modern javascripts to older javascripts which is compatible to all browsers

Role of Babel in JSX

In React we write JSX and JSX is js syntax by which we write React.createElements and Babel again converts JSX to React.createElements

```
const element = <h1>Hello, world!</h1>;

```

```
Babel Converts this to
const element = React.createElement('h1', null, 'Hello, world!');

```
17/01/2025


React is all about components -> We make components in React and use it everywhere.

There are two ways to create components
-> 1.Functioanl Componnets -> it used javascript function to create components
-> 2.Class Based Componnets -> it used javascript classes to create components

React Component is a javascript function.

Q.What is difference  b/w normal function and Functional Components?

normal function is normal function but Functional Components returns JSX(React elements);

React Element is javascript Objects.


Ways to write functional Components.


const HeadingComponents = () =>(
<div>
<h1>Hello from h1 div</h1>
<h2>Hello from 2nd div</h2>
</div>
);

const HeadingComponents = () =>{
return(
<div>
Hello from 2nd div
</div> 
)}

Q.What is difference between React Elements and React Components ?

root.reder(HeadingComponents) ❌
root.render(<HeadingComponents />)✅




Q.What is component Composition?

Combining two or more than  two  components -> This is called Component Composition.



SuperPower of JSX.

Inside JSX,we can run any javascript by wrapping  curly brackets {}

Writing javascript inside JSX is a magicla things in itself.It is very Powerfu things.

How can we write or inject React Elements insdie React Elements?
=> Inside curly brackets becaues Recat Elements is normal javascripts Objects.

Here Babel is palying a great role,it is converting Our JSX Code to React.createElement .

In react,When we store then it automaticlaly refresh the page => Parcel is doing all these things => we name these things as Hot module replacement => 
basically parcel tracks the file changes



```
 Very imp concept :: ==>  JSX Sanitize data or it doesn't blindly render data



const data = api.getData();

const Header = () =>{
return(
<div>
{data}
</div>
)}


Here data is coming from api,so it may be possible that attacker try to run some malicious javascripts in it but JSX will not render blindly it,
first of all it sanitizes the data coming from api and then it renders the data.

Its prevents cross site scripting.So,JSX is a very powerful things.



```

Suppose we make Header component then we call either it  as
1. <Header/>
2. {Header()}


One more things people say React make code more Readable but actually React doesn't make code readable.It's JSX that makes code more readable.









